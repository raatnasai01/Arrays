
def revesrearray(int,->arr):
        left=0
        right=len(arr)-1
        while left<right:
            arr[left],arr[right]=arr[right],arr[left]
            left+=1
            right-=1
        return arr

another solution in pyhton  using Inbuilt function method reverse()
return arr.reverse()
this command will execute that
